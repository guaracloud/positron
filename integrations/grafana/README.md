# Grafana integration

This directory owns the minimal host-required Grafana data source. It is a
product surface, never a runtime dependency or owner of database-domain
behavior. No integration code exists yet.
