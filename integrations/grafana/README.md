# Grafana integration scaffold

This directory is reserved for the minimal host-required Grafana data source.
It is a release-blocking artifact but never a runtime dependency or owner of
database-domain behavior. No integration code exists in the scaffold.
