# Distribution scaffold

This directory is reserved for native archives, packages, OCI, Compose, Nix,
Helm, Kubernetes, and example distribution surfaces. Each surface qualifies
the same built Positron artifact and storage format; none may rebuild or
silently change verified bytes.
