# Distribution surfaces

This directory is reserved for native archives, packages, OCI, Compose, Nix,
Helm, Kubernetes, and example distribution surfaces. Each surface packages the
same Positron artifact and storage format; none may rebuild or silently change
the product bytes.
