# Persistence adversarial corpus

This directory is reserved for golden, corrupt, truncated, unsupported, and
fault-boundary persistent objects. It remains empty until an accepted format
specification and publication-point registry exist.
