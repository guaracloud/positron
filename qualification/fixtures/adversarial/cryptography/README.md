# Cryptography adversarial corpus

This directory is reserved for published known-answer vectors and synthetic
wrong-key, wrong-context, substitution, corruption, restart, and lifecycle
cases. It must never contain a production key or credential.
